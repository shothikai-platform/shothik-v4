## 2024-07-01 - Missing ARIA labels in TipTapEditor\n**Learning:** The TipTapEditor component uses icon-only buttons for text formatting (Bold, Italic, Underline, Lists, etc.) without providing accessible names, which hinders screen reader users.\n**Action:** Add `aria-label` attributes to all icon-only formatting buttons to improve accessibility.
