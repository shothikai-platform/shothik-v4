1. modify `src/components/tools/paraphrase/OutputBotomNavigation.jsx` using `replace_with_git_merge_diff` to wrap the disabled icon buttons inside the tooltip triggers with `<span tabIndex={0} className="focus-visible:ring-2 focus-visible:ring-ring">`.
2. run `pnpm format` and `pnpm lint` and `pnpm test`
3. create PR with Palette title and description
